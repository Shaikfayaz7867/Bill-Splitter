# Server Configuration
PORT=5001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/bill-splitter

# Email Configuration
# For Gmail setup:
# 1. Enable 2-factor authentication in your Google account: https://myaccount.google.com/security
# 2. Generate an app password: Google Account > Security > App passwords
#    (Select "Mail" as the app and "Other" as the device)
# 3. Use that 16-character app password below instead of your regular Gmail password
EMAIL_USER=fayaz.ahamedwork@gmail.com
EMAIL_PASS=yvbsuemlqfgnwuxp
EMAIL_FROM_NAME=Bill Splitter

# Optional: SMTP configuration if not using Gmail
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_REJECT_UNAUTHORIZED=true

# For testing email functionality
TEST_EMAIL=test-recipient@example.com
